{
  "name": "moloch-rises-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth --product hosted-service 0xBeeDao/moloch-rises-subgraph",
    "deploy": "graph deploy --product hosted-service 0xBeeDao/moloch-rises-subgraph",
    "create-local": "graph create --node http://localhost:8020/ moloch-rises-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ moloch-rises-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 moloch-rises-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.20.0",
    "@graphprotocol/graph-ts": "^0.20.0"
  }
}
